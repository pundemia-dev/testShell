import qs.components.misc

// Manifest for the Workspaces bar widget. The implementation (Workspaces.qml) is
// loaded by URL (WidgetHost / palette previews); the manifest only carries
// palette metadata.
PluginManifest {
    title: "Workspaces"
    icon: ""
    order: 10
}
